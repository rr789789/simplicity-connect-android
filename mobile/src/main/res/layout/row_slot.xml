<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="174dp"
    android:layout_margin="4dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    app:cardBackgroundColor="?attr/appSurface"
    app:cardCornerRadius="10dp"
    app:cardElevation="0dp"
    app:strokeColor="?attr/appOutline"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="12dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="30dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/slotNumber"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="槽 1"
                android:textColor="?attr/appTextPrimary"
                android:textSize="14sp"
                android:textStyle="bold" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="28dp"
                android:layout_marginStart="6dp"
                android:background="@drawable/bg_panel_raised"
                android:orientation="horizontal"
                android:padding="2dp">

                <TextView android:id="@+id/slowRate" android:layout_width="30dp" android:layout_height="24dp" android:gravity="center" android:text="慢" android:textColor="?attr/appTextSecondary" android:textSize="11sp" />
                <TextView android:id="@+id/fastRate" android:layout_width="30dp" android:layout_height="24dp" android:gravity="center" android:text="快" android:textColor="?attr/appTextSecondary" android:textSize="11sp" />
            </LinearLayout>

            <View android:layout_width="0dp" android:layout_height="1dp" android:layout_weight="1" />

            <TextView
                android:id="@+id/state"
                android:layout_width="wrap_content"
                android:layout_height="28dp"
                android:background="@drawable/bg_panel_raised"
                android:gravity="center"
                android:minWidth="62dp"
                android:paddingHorizontal="8dp"
                android:text="未绑定"
                android:textColor="?attr/appTextSecondary"
                android:textSize="12sp"
                android:textStyle="bold" />
        </LinearLayout>

        <TextView
            android:id="@+id/value"
            android:layout_width="match_parent"
            android:layout_height="41dp"
            android:gravity="center_vertical"
            android:maxLines="1"
            android:text="--"
            android:textColor="?attr/appTextPrimary"
            android:textSize="18sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/position"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="未配置位置"
            android:textColor="?attr/appTextSecondary"
            android:textSize="12sp" />

        <TextView
            android:id="@+id/diagnostics"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="7dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="电压 --  RSSI --  PA5 --"
            android:textColor="?attr/appTextSecondary"
            android:textSize="12sp" />

        <TextView
            android:id="@+id/updated"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="bottom|end"
            android:text="无数据"
            android:textColor="?attr/appTextSecondary"
            android:textSize="11sp" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
